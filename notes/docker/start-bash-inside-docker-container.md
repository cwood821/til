# Start Bash session inside a docker container

To start a Bash session inside a docker container:

```bash
docker exec -it name-of-container bash
```

This assumes the container has bash available.



